# Static analysis stub removed; use node_resolvers_shim.gd for symbol resolution.
# This file intentionally left minimal to avoid duplicate `class_name NodeResolvers` conflicts introduced during refactor.

# NOTE: Do not declare `class_name NodeResolvers` here; the real shim is `node_resolvers_shim.gd`.
