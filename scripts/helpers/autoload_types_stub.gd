# Autoload types stub intentionally empty to avoid creating `class_name` symbols
# during refactor. Real autoloads are provided by project settings and
# runtime scripts. This file previously contained `class_name` entries
# which caused the engine to complain about class names hiding autoload
# singletons; that produced the oscillating ERROR state. Keep this file
# minimal so it doesn't affect runtime or static analysis.

# If you need static-analysis helpers, create a separate file with
# unique class names (e.g. ThemeManagerStub) that will not collide with
# autoload names.
